# Local only
PLAN.md
.claude
.envs

# Personal notes — never leave this machine
.lore/local/

# Node
node_modules/
*.vsix
*.log

# Build output. Rooted, so plugin/dist and mcp/dist stay tracked on purpose:
# `/plugin marketplace add` clones this repository and runs what it finds,
# with no install or build step of its own.
/out/
/dist/

# OS
.DS_Store
Thumbs.db

# Packaged extension — rebuild with `npx @vscode/vsce package`
*.vsix
